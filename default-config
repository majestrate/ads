
node {
  listen {
    host = "::"
    port = 63763
  }

  http {
    host = "localhost"
    port = 8080
  }
}

logging {
  
}

freenet {
  datastore {
    directory = "$(HOME)/.ads/data-store"
    chk-count = 300000
    ssk-count = 300000
  }
  
}
